<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="./css/serach_faculty.css">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <title>Document</title>
</head>

<body>
    <header>
        <h1>FacultyHub</h1>
        <nav>
            <a href="#">Home</a>
            <a href="#">University</a>
            <a href="#">About Us</a>
            <a href="#">Login/Sign up</a>
        </nav>
    </header>


    <form id="searchForm" action="/search-faculty" method="post">
        <input type="text" placeholder="Search.." name="search2" value="{{searchdata.search_query}}">
        <br>
        {{!-- <label for="location">location:</label>
        <input type="text" name="location" id="location">
        <br> --}}
        Course:
        <input type="text" name="course">
        <br>
        <label for="university">University:</label>
        <select name="university" id="university">
            <option value="all">ALL</option>
            <option value="all">daiict</option>
            <option value="all">nirma</option>
            {{!-- <option value="daiict" {{#if (eq serachdata.university2 "daiict")}}selected{{/if}}>daiict</option>
            <option value="nirma" {{#if (eq serachdata.university2 "nirma")}}selected{{/if}}>nirma</option> --}}

            {{#each universityoption}}
                <option value="{{this.university}}" {{#if (eq ../searchdata.university
                    this.university)}}selected{{/if}}>{{this.university}}</option>
            {{/each}}
        </select>

        <br>
        <button type="submit" onclick="updateAction()"><i class="fa fa-search"></i></button>
    </form>




    <div>
        <div class="album py-5 bg-body-tertiary">
            <div class="container">
                <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
                    {{#each data}}
                        <div class="col">
                            <div class="card shadow-sm">
                                <svg class="bd-placeholder-img card-img-top" width="100%" height="225"
                                    xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Placeholder: Thumbnail"
                                    preserveAspectRatio="xMidYMid slice" focusable="false">
                                    <title>Placeholder</title>
                                    <rect width="100%" height="100%" fill="#55595c"></rect><text x="50%" y="50%"
                                        fill="#eceeef" dy=".3em">Thumbnail</text>
                                </svg>
                                <div class="card-body">
                                    <p class="card-text"> {{this.name}}</p>
                                    <p class="card-text"> {{this.university}} </p>
                                    <ul>
                                        <li>{{this.email}}</li>
                                        <li>{{this.mobile_no}}</li>
                                        {{!-- <li>Adress</li>
                                    <li>Website</li> --}}
                                    </ul>

                                    <div class="d-flex justify-content-between align-items-center">
                                        <div class="btn-group">
                                            <!-- <button type="button" class="btn btn-sm btn-outline-secondary">View</button>
                          <button type="button" class="btn btn-sm btn-outline-secondary">Edit</button> -->
                                        </div>
                                        <small class="text-body-secondary">9 mins</small>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {{else}}
                        <p>Your search yielded no results. </p>
                    {{/each}}
                </div>

            </div>
        </div>
    </div>

    <footer>
        <div class="left">
            <h2>Important Links</h2>
            <a href="#">Home</a>
            <a href="#">University</a>
            <a href="#">About Us</a>
            <a href="#">Login/Sign up</a>
        </div>
        <div class="right">
            <h2>About the platform</h2>
            <p>This platform provides information of professional achievement and contact of faculties of multiple
                universities to help knowledge seekers and professionals to connect.</p>
        </div>
    </footer>
</body>

<script>

    function updateAction() {

        var nameInput = document.getElementById("searchForm").elements["search2"];
        var locationInput = document.getElementById("searchForm").elements["location"];
        var courseInput = document.getElementById("searchForm").elements["course"];
        var universitySelect = document.getElementById("searchForm").elements["university"];

        // Check if the "s" query parameter is empty and remove it
        if (nameInput.value === "") {
            nameInput.removeAttribute("name");
        }

        // Check if the "course" query parameter is empty and remove it
        if (courseInput.value === "") {
            courseInput.removeAttribute("name");
        }

        // Check if the "university" is not selected, and remove it if it's empty
        if (universitySelect.selectedIndex === 0) {
            universitySelect.removeAttribute("name");
        }
    }






</script>

</html>